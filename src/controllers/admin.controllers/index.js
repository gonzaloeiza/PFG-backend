const authController = require("./auth.controllers");
const usersController = require("./users.controllers");
const bookingsController = require("./bookings.controllers");

module.exports = {
    authController,
    usersController,
    bookingsController,
}